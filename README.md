# Pro Merchant Savings — Website

Conversion-focused marketing site for **Pro Merchant Savings**, an independent merchant services
consultancy that helps small and mid-sized businesses lower credit card processing costs.

- **Phone (click-to-call):** [(614) 419-1601](tel:+16144191601)
- **Email:** [promerchantsavings@gmail.com](mailto:promerchantsavings@gmail.com)

## Stack

Vanilla HTML, CSS and JavaScript — no build step, no dependencies, no external APIs.
Open `index.html` in a browser or serve the folder statically (e.g. `python3 -m http.server`).

## Pages

| File | Purpose |
| --- | --- |
| `index.html` | Hero + CTA, services overview, process, why us, reviews, FAQ, quote form |
| `services.html` | Detailed services and industries served |
| `about.html` | Approach, promises, transparency on compensation, full reviews |
| `faq.html` | Full FAQ grouped by pricing, programs, setup and support (FAQPage schema) |
| `contact.html` | Contact details, hours, service area and the quote request form |

## Structure

```
index.html services.html about.html faq.html contact.html
css/styles.css      all styling (design tokens, responsive layout, components)
js/main.js          mobile nav, FAQ accordion, form validation, scroll reveal
assets/favicon.svg  favicon / logo mark placeholder
site.webmanifest robots.txt sitemap.xml
```

## Notes

- The quote forms (homepage and contact page) are client-side validated and posted to
  `/api/ghl-lead`, which creates or updates the contact in the GoHighLevel sub-account
  `MD8tHnfr4XTmcDJIzqbk` with first name, last name, phone, email and the message, sets the
  custom fields **Lead Source** = `Website` and **Website Form** = the form name, and applies
  the tag `website-lead`. A thank-you message is shown in place after a successful submit.
- Required environment variable on the deployment: `GHL_API_KEY` — a GoHighLevel Private
  Integration / OAuth access token scoped to that sub-account (`GHL_ACCESS_TOKEN` and
  `GOHIGHLEVEL_API_KEY` are accepted aliases). Optional: `GHL_LOCATION_ID` to override the
  location, `GHL_WEBHOOK_URL` to deliver via a GoHighLevel inbound webhook instead.
- Sticky click-to-call bar on mobile; the phone number also appears in the top bar, hero, footer and CTAs.
- Semantic HTML, skip link, ARIA labels, reduced-motion support and print styles included.

_Website generated by Goose Site Builder._
